<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
   
    <div id="webapp">
    <h1>Weather App</h1>
    </div>



    <form action="weather" class="form">
        <input type="text" id="texto" name="texto" placeholder="Escreva o seu local aqui!"/>
        <button type="submit" id="botao" value=""> Pesquisar</button>
        <button type="button" id="favoritos" value="" onclick="myFunction()"> Favoritos</button>


    </form>

    <form class="form-horizontal">
        <button type="submit" id="adicionar" class="btn btn-primary">Adicionar</button>
        <button type="reset" id="clean"  class="btn btn-secondary">Limpar lista</button>
        </div>
      </form>

 <li class="fav">
       
    </li>


 <script
        src="https://code.jquery.com/jquery-3.3.1.min.js"
        integrity="sha256-FgpCb/KJQlLNfOu91ta32o/NMZxltwRo8QtmkMRdAu8="
        crossorigin="anonymous"></script>
    <script>

    $(document).ready(function () {

       $("#shopping-list").hide();
       $("#item").focus();

      /*
       * check browser supports local storage
       */
      if (localStorage) {

        var shoppingList_string = localStorage.getItem('shoppingList');
        if (shoppingList_string) {
          $("#shooping-list-text").hide();
          $("#shopping-list").show();
          var shoppingList = JSON.parse(shoppingList_string);
          console.log(shoppingList);



          $.each(shoppingList, (index, text) => {

           
            $("#shopping-list").append(`<li class="list-group-item">${text}</li>`);
          });
        
        
        }

        
        $("form")
           document.getElementById("adicionar").onclick(function(){
            var shoppingList_string = localStorage.getItem('shoppingList');
            var shoppingList =[];

            if(shoppingList_string) {
              shoppingList = JSON.parse(shoppingList_string);
            }

            var filteredList= shoppingList.filter((item)=>{return item===$("#item").val();});
            if(filteredList.length===0){
            shoppingList.push($("#item").val());
            localStorage.setItem('shoppingList', JSON.stringify(shoppingList));  
            }
           });
      }
    });
    </script>

</body>
</html>